/**
 * Removed on 2026-09-03. A browser store is not somewhere a knitter's work
 * should live, and putting a manager around it invites people to trust it.
 * The working chart survives a reload through sessionStorage with no UI, and
 * saving a pattern means printing it or exporting the JSON.
 *
 * This file is unreferenced and can be deleted.
 */
export {};
