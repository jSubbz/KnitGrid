/**
 * Replaced by MyPatternsPage on 2026-09-03. "Library" is reserved for a shared
 * catalogue of patterns other knitters have published, which does not exist
 * yet and needs a server rather than a page. This file is unreferenced and can
 * be deleted.
 */
export {};
